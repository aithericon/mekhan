<?xml version="1.0" encoding="UTF-8" ?>
<!-- Force Fast DDS onto plain UDPv4. Default builtin transports include
     shared memory, which two same-host containers negotiate (they see each
     other via host networking) but cannot actually use (each has a private
     /dev/shm) — participants discover each other and then exchange nothing.
     Mounted into BOTH the isaac and xarm containers via
     FASTRTPS_DEFAULT_PROFILES_FILE. -->
<profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
  <transport_descriptors>
    <transport_descriptor>
      <transport_id>udp_only</transport_id>
      <type>UDPv4</type>
    </transport_descriptor>
  </transport_descriptors>
  <participant profile_name="udp_only_participant" is_default_profile="true">
    <rtps>
      <userTransports>
        <transport_id>udp_only</transport_id>
      </userTransports>
      <useBuiltinTransports>false</useBuiltinTransports>
    </rtps>
  </participant>
</profiles>
