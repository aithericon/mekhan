# Copy to backend.hcl (gitignored). These values match the prod environment
# of the HetznerCluster repo (environments/prod/env.hcl).
#
# AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY come from environment variables
# (Woodpecker secrets tf_state_access_key_prod / tf_state_secret_key_prod or
# the same shared pair if your operator chooses) — do not commit them here.

endpoint = "https://fsn1.your-objectstorage.com"
bucket   = "tfstate-aithericon-prod"
region   = "fsn1"
