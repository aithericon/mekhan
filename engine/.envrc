# >>> shared-cargo-target (managed by setup-shared-target.sh) >>>
# Shares the cargo target dir across all worktrees of this workspace.
# Delete this block and run 'direnv reload' to opt out.
export CARGO_TARGET_DIR="$HOME/.cache/cargo-targets/aithericon-platform/engine"
# <<< shared-cargo-target <<<
