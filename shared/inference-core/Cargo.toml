[package]
name = "inference-core"
version = "0.1.0"
edition = "2021"
license = "FSL-1.1-ALv2"
description = "Shared OpenAI-compatible wire DTOs for the inference router (doc 11/29)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
