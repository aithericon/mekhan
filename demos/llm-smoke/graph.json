{
  "nodes": [
    {
      "id": "start",
      "type": "start",
      "position": { "x": 0, "y": 100 },
      "data": {
        "type": "start",
        "label": "Start",
        "processName": "LLM smoke test",
        "initial": {
          "id": "in",
          "label": "Begin",
          "fields": []
        }
      }
    },
    {
      "id": "ask",
      "type": "agent",
      "position": { "x": 190, "y": 0 },
      "data": {
        "type": "agent",
        "label": "Ask LLM",
        "description": "Send a fixed prompt through the internal LLM pool (the in-cluster inference router bound via `internal_pool_router`) and capture the response. Uses `llama3.2:1b` — the small/fast model pulled by default `just dev` (DEMO36_MODEL) and seeded `loaded` in the pool — so the smoke test runs out of the box without a heavy 9B download. The model must be loaded resident on a runner for the pool's live-runner gate to report it available.",
        "model": {
          "provider": "internal",
          "model": "llama3.2:1b",
          "resourceAlias": "internal_pool_router",
          "maxTokens": 256,
          "temperature": 0
        },
        "userPrompt": "Write a single sentence that explains what Ollama is to a software engineer.",
        "maxTurns": 1
      }
    },
    {
      "id": "end",
      "type": "end",
      "position": { "x": 520, "y": 68 },
      "data": {
        "type": "end",
        "label": "Done",
        "resultMapping": [
          {
            "targetField": "response",
            "expression": "ask.response"
          }
        ]
      }
    }
  ],
  "edges": [
    {
      "id": "e_start_ask",
      "source": "start",
      "target": "ask",
      "targetHandle": "in",
      "type": "sequence"
    },
    {
      "id": "e_ask_end",
      "source": "ask",
      "target": "end",
      "targetHandle": "in",
      "type": "sequence",
      "sourceHandle": "out"
    }
  ]
}
