{
  "name": "ros",
  "fields": [
    { "name": "robot_model", "kind": "text", "required": true },
    { "name": "ros_distro", "kind": "select", "required": true, "options": ["humble", "iron", "jazzy", "kilted", "rolling"] }
  ]
}
